# fastapi-jwt-auth-examples
 Two FastAPI implementations of JWT-based authentication with SQLite + bcrypt — one using a custom header token, the other using standard OAuth2PasswordBearer. Built while learning auth patterns in FastAPI.
